export interface LoadingProps {
    isLoading: boolean;
    setIsLoading: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface SetLoadingProp {
    setIsLoading: React.Dispatch<React.SetStateAction<boolean>>;
}